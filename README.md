# ClickFlix

A minimal Flutter starter app scaffolded by Copilot assistant.

## Quick start

1. Make sure Flutter SDK is installed and on your PATH.
2. From the project root run:

```powershell
flutter pub get
flutter run
```

3. To open in VS Code, ensure the Flutter and Dart extensions are installed.

