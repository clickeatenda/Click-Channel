name: clickflix
description: App de Streaming IPTV.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Pacotes Essenciais (Alinhamento é vital aqui)
  http: ^1.2.0
  video_player: ^2.8.2
  chewie: ^1.7.1
  cached_network_image: ^3.3.1
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true