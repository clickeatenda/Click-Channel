# Copy this file to `.env` and edit values for your environment

# Backend base URL used by the app (do not include trailing slash)
BACKEND_URL=http://192.168.3.251:4000

# Optional: M3U playlist URL for client-side parsing (front-only flow)
# Example: https://my-server.com/playlist.m3u or file:///storage/emulated/0/Download/playlist.m3u
M3U_PLAYLIST_URL=

# Optional: curated featured JSON URL. If set, the app will prefer these over automatic daily selection.
# Expected JSON format:
# {
#   "movie": ["http://.../movie1.ts", "http://.../movie2.ts"],
#   "series": ["http://.../series1.m3u8", ...],
#   "channel": ["http://.../channel1.m3u8", ...]
# }
FEATURED_JSON_URL=

# TMDB API Key (v3) - required for ratings/enrichment features
# Obtain an API key from https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# Jellyfin Server Configuration (opcional)
# Configure se desejar integrar conteúdo de um servidor Jellyfin
JELLYFIN_URL=http://192.168.1.100:8096
JELLYFIN_USERNAME=seu_usuario
JELLYFIN_PASSWORD=sua_senha
JELLYFIN_LIBRARY_ID=  # ID específico da biblioteca (opcional, deixe vazio para todas)

# Notes:
# - Nunca comite seu arquivo .env com chaves reais no repositório.
# - Se uma chave foi acidentalmente comprometida no histórico do Git, roteie a chave imediatamente e remova-a do histórico.
# - Em CI/CD, configure TMDB_API_KEY como um segredo no pipeline em vez de colocá-lo no repositório.
